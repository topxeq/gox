var tk = import("tk")

tk.Pl("Args: %#v", inG.Args)

x = inG
y = x + 1

// print using outside the script defined println function
tk.Pl("%v", x + y) // 3

s = ""+(x+y)

tk.Prl(s)

resultG = [x, y]

var regexp = import("regexp")

for s in regexp.MustCompile(`[\s_]`).Split("foo_bar_baz", -1) {
  println(s)
}

var os = import("os")
var runtime = import("runtime")

println(runtime.GOOS)

a = []interface{1, 2}
println(a)

a += 3
println(a)

a += "3"
println(a)

a += "a378d"
println(a)

a += true
println(a)

println(a[1] + a[2])
println(a[1] + a[4])

setVar("abc", a)

println(getVar("abc"))

countT = 0

func loopPrint(idA) {
    for i = 0; i < 100; i++ {
        println(idA, ": ", i)
    }

    countT ++
}

go loopPrint(1)
go loopPrint(2)

for {
    if countT >= 2 {
        // tk.Pl("countT: %v", countT)
        break
    }

    tk.SleepSeconds(1)
}

var dialog = import("dialog")

dialog.Message("%s", "Do you want to continue?").Title("Are you sure?").YesNo()