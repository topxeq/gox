// 单行的注释，以两个斜杠字符“/”开始，范围到本行结束为止

a = 1 // 单行的注释也可以从一行的中间开始，这时候“//”之前的仍然是有效代码，之后的才是注释

/* 
多行的注释与C语言一样，
用“/*”标志开始，
用“* /”（中间没有空格）标志结束。
*/

pln(a + a)

a = 18

for a > 0 {
	pln("a=", a);

	a = a - 1

	/*
	a = a + 2
	a = b
	*/
}